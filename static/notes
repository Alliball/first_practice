git init # initialize the repo

git status # tells you about changes
           # since the last commit

git add [filename] [filename] ... # files
git add. # all the changed files

make the commit:
git commit -m "a description of the changes"